(library
 (name mineplex_base_unix)
 (public_name mineplex-base.unix)
 (flags (:standard -open mineplex_error_monad
                   -open mineplex_crypto
                   -open mineplex_base
                   -open mineplex_stdlib_unix
                   -open Data_encoding))
 (libraries data-encoding
            mineplex-crypto
            mineplex-base
            mineplex-stdlib-unix
            mineplex-error-monad))

(alias
 (name runtest_lint)
 (deps (glob_files *.ml{,i}))
 (action (run %{lib:mineplex-tooling:lint.sh} %{deps})))

