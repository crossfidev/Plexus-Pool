(library
 (name mineplex_stdlib)
 (public_name mineplex-stdlib)
 (libraries hex
            zarith
            lwt)
 (flags :standard))

(alias
 (name runtest_lint)
 (deps (glob_files *.ml{,i}))
 (action (run %{lib:mineplex-tooling:lint.sh} %{deps})))
