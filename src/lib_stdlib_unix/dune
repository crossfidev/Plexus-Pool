(library
 (name mineplex_stdlib_unix)
 (public_name mineplex-stdlib-unix)
 (flags (:standard -open mineplex_error_monad
                   -open mineplex_event_logging
                   -open mineplex_stdlib
                   -open Data_encoding))
 (libraries data-encoding
            mineplex-error-monad
            mineplex-event-logging
            mineplex-stdlib
            lwt.unix
            ipaddr.unix
            ptime
            ptime.clock.os
            mtime
            mtime.clock.os
            lwt_log))

(alias
 (name runtest_lint)
 (deps (glob_files *.ml{,i}))
 (action (run %{lib:mineplex-tooling:lint.sh} %{deps})))
