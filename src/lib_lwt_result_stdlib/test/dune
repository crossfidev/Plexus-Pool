(executables
 (names test_hashtbl)
 (libraries mineplex-lwt-result-stdlib
            mineplex-error-monad
            lwt.unix
            alcotest-lwt)
 (flags (:standard -open mineplex_lwt_result_stdlib)))

(alias
 (name buildtest)
 (deps test_hashtbl.exe))

(alias
 (name runtest_hashtbl)
 (action (run %{exe:test_hashtbl.exe})))

(alias
 (name runtest)
 (package mineplex-lwt-result-stdlib)
 (deps (alias runtest_hashtbl)))
