(library
 (name mineplex_storage)
 (public_name mineplex-storage)
 (libraries mineplex-base
            mineplex-shell-services
            mineplex-lmdb
            logs.fmt
            digestif.c
            irmin
            irmin-pack
            mineplex-stdlib-unix)
 (flags (:standard -open mineplex_shell_services
                   -open mineplex_base__TzPervasives
                   -open mineplex_stdlib_unix
                   -open mineplex_stdlib)))

(alias
 (name runtest_lint)
 (deps (glob_files *.ml{,i}))
 (action (run %{lib:mineplex-tooling:lint.sh} %{deps})))
